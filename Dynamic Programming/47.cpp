/*
    problem: you are given an array. find the number of longest increasing 
    subsequence
    
*/

#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    
    vector<int>v(n),dp(n,1),numberOfLCS(n,0);
    for(auto &it:v)
    cin>>it;

    for(int i=0;i<n;i++)
    {

    }

}