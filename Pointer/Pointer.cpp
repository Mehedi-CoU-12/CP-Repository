/*
================================================================================

                      C++ POINTER — সম্পূর্ণ নোট
                        (একদম Zero থেকে Advanced)

  এই ফাইলটা এমনভাবে লেখা, যেন ১ বছর পরে কিছুই মনে না থাকলেও
  উপর থেকে নিচ পর্যন্ত পড়লেই pointer পুরোপুরি বুঝে যাও।
  আগে থেকে কোনো knowledge লাগবে না — শুধু C++ এর variable আর
  function চিনলেই যথেষ্ট।

  কীভাবে পড়বে:
      - উপর থেকে নিচে ক্রমানুসারে পড়ো, PART লাফ দিয়ে যেয়ো না
      - প্রতিটা code নিজে হাতে টাইপ করে run করো
      - প্রতি PART শেষে "মনে রাখো" বক্সটা একবার জোরে পড়ো
      - আটকে গেলে কাগজে বক্স এঁকে address আর value লিখে ফেলো

================================================================================
  সূচিপত্র  (TABLE OF CONTENTS)
================================================================================

  PART 0   Memory, Variable, Address — pointer-এর আগের গল্প
  PART 1   Pointer কী?  ( &  আর  *  — সবকিছুর মূল )
  PART 2   Pointer declaration-এর নিয়ম আর ফাঁদ
  PART 3   NULL pointer, uninitialized pointer, dangling pointer
  PART 4   Pointer আর Function  (pass by value / pointer / reference)
  PART 5   Pointer আর Array  (pointer arithmetic)
  PART 6   Pointer আর String  ( char* )
  PART 7   Pointer আর struct / class  (  ->  operator )
  PART 8   Pointer to Pointer  ( int **pp )
  PART 9   Dynamic Memory — new / delete  (Heap vs Stack)
  PART 10  const pointer vs pointer to const
  PART 11  void pointer
  PART 12  Function pointer
  PART 13  this pointer  (OOP)
  PART 14  Pointer vs Reference — কোনটা কখন?
  PART 15  Smart Pointer  (Modern C++ — unique_ptr / shared_ptr)
  PART 16  Iterator = বড় ভাই pointer  (STL-এ pointer)
  PART 17  Competitive Programming-এ pointer আসলে কোথায় লাগে
  PART 18  সাধারণ ভুলের লিস্ট  (Common Mistakes)
  PART 19  শেষ চিটশিট — এক পাতায় সব  (Cheat Sheet)
  PART 20  অনুশীলন প্রশ্ন + উত্তর  (Practice)


================================================================================
  ৬০ সেকেন্ডের রিভিশন
  (১ বছর পরে শুধু এইটুকু পড়লেও ৭০% মনে পড়ে যাবে)
================================================================================

      int x = 10;        // সাধারণ variable — ভিতরে value থাকে
      int *p = &x;       // pointer — ভিতরে x-এর ADDRESS থাকে
      cout << *p;        // 10  <- address-এ গিয়ে value নিয়ে এলাম

      &x   =  "x-এর address দাও"            (address-of)
      *p   =  "p যেই address ধরে আছে,       (dereference)
               সেখানে গিয়ে value দাও"

      p    ->  address
      *p   ->  value

      মূল কথা:  pointer হলো এমন একটা variable, যার ভিতরে সংখ্যা নয়,
                বরং অন্য variable-এর ঠিকানা (address) থাকে।

  এবার একদম শুরু থেকে ধীরে ধীরে বুঝি।


################################################################################
#  PART 0 — MEMORY, VARIABLE, ADDRESS
#           (pointer বোঝার আগে এই জিনিসটা বুঝতেই হবে)
################################################################################

--------------------------------------------------------------------------------
  ০.১  Computer-এর memory দেখতে কেমন?
--------------------------------------------------------------------------------

  RAM-কে ভাবো একটা বিশাল হোটেল, যার প্রতিটা রুমের একটা নম্বর আছে।

      +--------+--------+--------+--------+--------+
      |  1000  |  1001  |  1002  |  1003  |  1004  |   <- রুম নম্বর (address)
      +--------+--------+--------+--------+--------+
      |        |        |        |        |        |   <- রুমের ভিতরের জিনিস
      +--------+--------+--------+--------+--------+                    (value)

      - রুম নম্বর            =  ADDRESS  (কখনো বদলায় না)
      - রুমের ভিতরের জিনিস  =  VALUE    (যখন খুশি বদলানো যায়)


--------------------------------------------------------------------------------
  ০.২  Variable বানালে আসলে ভিতরে কী হয়?
--------------------------------------------------------------------------------

      int x = 10;

  এই এক লাইনে আসলে ৩টা কাজ হয়:

      ১. memory-তে ৪ byte জায়গা (৪টা রুম) দখল হয়
      ২. সেই জায়গার একটা address আছে, ধরি 1000
      ৩. ওই জায়গায় 10 লিখে রাখা হয়, আর নাম দেওয়া হয়  x

      নাম (x)  ->  address (1000)  ->  value (10)

  তুমি যখন শুধু  x  লিখো, compiler গোপনে "1000 নম্বর রুমে যাও" কাজটা
  করে ফেলে। Pointer শেখা মানে এই গোপন কাজটা নিজের হাতে নিয়ে নেওয়া।


--------------------------------------------------------------------------------
  ০.৩  Address নিজের চোখে দেখো
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    int main() {
        int x = 10;

        cout << "value   : " << x  << endl;   // 10
        cout << "address : " << &x << endl;   // 0x7ffd... (hex-এ address)

        return 0;
    }

  প্রতিবার run করলে address বদলে যাবে — এটা স্বাভাবিক, ভয় পেয়ো না।
  OS প্রতিবার আলাদা জায়গা দেয়।


--------------------------------------------------------------------------------
  ০.৪  কোন type কত byte নেয়?  (pointer arithmetic-এ কাজে লাগবে)
--------------------------------------------------------------------------------

  | Type      | Size (সাধারণত) |
  |-----------|----------------|
  | char      | ১ byte         |
  | bool      | ১ byte         |
  | int       | ৪ byte         |
  | float     | ৪ byte         |
  | double    | ৮ byte         |
  | long long | ৮ byte         |

  নিজে দেখতে চাইলে:   cout << sizeof(int);


  +----------------------------------------------------------------------+
  |  মনে রাখো (PART 0)                                                   |
  |  প্রতিটা variable-এর দুইটা পরিচয় আছে:  VALUE  আর  ADDRESS           |
  +----------------------------------------------------------------------+


################################################################################
#  PART 1 — POINTER কী?  (  &  আর  *  )
################################################################################

--------------------------------------------------------------------------------
  ১.১  এক লাইনের definition
--------------------------------------------------------------------------------

  Pointer হলো এমন একটা variable, যেটা value ধরে রাখে না,
  বরং অন্য একটা variable-এর ADDRESS ধরে রাখে।

  সহজ উদাহরণ:

      x   =  তোমার বাসা (ভিতরে জিনিসপত্র আছে)
      p   =  কাগজে লেখা তোমার বাসার ঠিকানা

      কাগজটা বাসা নয়, কিন্তু কাগজ দেখে বাসায় পৌঁছানো যায়।
      ঠিক তেমনি pointer value নয়, কিন্তু pointer দিয়ে value-তে পৌঁছানো যায়।


--------------------------------------------------------------------------------
  ১.২  মাত্র দুইটা operator — এই দুইটাই pointer-এর সব
--------------------------------------------------------------------------------

  | Operator | নাম          | কাজ                                       |
  |----------|--------------|-------------------------------------------|
  | &        | address-of   | variable-এর address দেয়                  |
  | *        | dereference  | address-এ গিয়ে value দেয় (বা বদলায়)    |

  &  আর  *  একটা আরেকটার ঠিক উল্টো:

      &x  ->  x থেকে address পাওয়া যায়
      *p  ->  address থেকে value পাওয়া যায়


--------------------------------------------------------------------------------
  ১.৩  জীবনের প্রথম pointer program (লাইন ধরে ধরে)
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    int main() {
        int x = 10;        // সাধারণ variable
        int *p = &x;       // p একটা pointer, x-এর address ধরে রাখলো

        cout << x   << endl;   // 10      <- x-এর value
        cout << &x  << endl;   // 0x1000  <- x-এর address
        cout << p   << endl;   // 0x1000  <- p-এর ভিতরে x-এর address (একই!)
        cout << &p  << endl;   // 0x2000  <- p নিজেও variable, তারও address আছে
        cout << *p  << endl;   // 10      <- p-এর address-এ গিয়ে value আনলাম

        return 0;
    }

  এই লাইনটা ভেঙে দেখো:

      int *p = &x;
      |    |    |
      |    |    +--  x-এর address নাও
      |    +-------  p নামে একটা pointer বানাও
      +------------  p শুধু int-এর address ধরবে (অন্য type-এর নয়)


--------------------------------------------------------------------------------
  ১.৪  ছবিতে দেখো
--------------------------------------------------------------------------------

                  x                        p
            +-----------+            +-----------+
            |    10     |            |   1000    |
            +-----------+            +-----------+
            address 1000             address 2000
                  ^                        |
                  |________________________|
                        p, x-কে point করছে

      x   = 10       (আসল value)
      &x  = 1000     (x কোথায় আছে)
      p   = 1000     (p-এর ভিতরে x-এর address)
      &p  = 2000     (p নিজে কোথায় আছে)
      *p  = 10       (1000 নম্বরে গিয়ে যা পেলাম)


--------------------------------------------------------------------------------
  ১.৫  Pointer দিয়ে value বদলানো  (আসল শক্তি এখানেই)
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    int main() {
        int x = 10;
        int *p = &x;

        *p = 50;        // x-কে একবারও ছুঁইনি, তবুও x বদলে গেলো

        cout << x   << endl;   // 50
        cout << *p  << endl;   // 50

        return 0;
    }

  কারণ:  *p  আর  x  — দুইটাই একই memory-র কথা বলছে।
         তাই একটা বদলালে আরেকটাও বদলায়।

  এটাই pointer-এর মূল ব্যবহার — দূর থেকে অন্যের data বদলানো।


--------------------------------------------------------------------------------
  ১.৬  Pointer-এর size কত?
--------------------------------------------------------------------------------

    int    *p1;
    double *p2;
    char   *p3;

    cout << sizeof(p1);   // 8
    cout << sizeof(p2);   // 8
    cout << sizeof(p3);   // 8

  সব pointer-এর size একই (64-bit machine-এ ৮ byte)।

  কারণ: pointer তো data রাখে না, শুধু একটা ঠিকানা রাখে।
        আর ঠিকানা লিখতে যত জায়গা লাগে, সেটা data type-এর উপর
        নির্ভর করে না।

  তাহলে type কেন লিখতে হয়? দুইটা কারণে —

      ১. *p করলে কত byte পড়বে সেটা জানার জন্য (int হলে ৪, double হলে ৮)
      ২. p+1 করলে কত byte সামনে যাবে সেটা জানার জন্য (PART 5-এ আসছে)


  +----------------------------------------------------------------------+
  |  মনে রাখো (PART 1)                                                   |
  |      p   ->  address                                                 |
  |      *p  ->  value                                                   |
  |      *p = 5;   মানে যাকে point করছে, তার value 5 করে দাও             |
  +----------------------------------------------------------------------+


################################################################################
#  PART 2 — POINTER DECLARATION-এর নিয়ম আর ফাঁদ
################################################################################

--------------------------------------------------------------------------------
  ২.১  তিনভাবে লেখা যায় — তিনটাই একই জিনিস
--------------------------------------------------------------------------------

    int* p;      // স্টাইল ক — "p হলো int-pointer" (type-এর সাথে * )
    int *p;      // স্টাইল খ — সবচেয়ে বেশি ব্যবহৃত, নিচের ফাঁদ এড়ায়
    int * p;     // স্টাইল গ — কেউ লেখে না

  Compiler-এর কাছে তিনটাই সমান। তুমি  int *p  স্টাইলে থাকো, কারণ —


--------------------------------------------------------------------------------
  ২.২  ফাঁদ ১: এক লাইনে দুইটা pointer  (এখানে সবাই ভুল করে)
--------------------------------------------------------------------------------

    int* p, q;      // [X] ফাঁদ! p pointer, কিন্তু q সাধারণ int
    int *p, q;      // একই জিনিস, তবে এখানে ভুলটা চোখে পড়ে

  ঠিকভাবে দুইটা pointer বানাতে:

    int *p, *q;     // [OK] দুইটাই pointer
    int *p;         // [OK] সবচেয়ে পরিষ্কার — আলাদা লাইনে লেখো
    int *q;

  কারণ:  *  type-এর সাথে যায় না,  variable-এর নামের সাথে যায়।


--------------------------------------------------------------------------------
  ২.৩  ফাঁদ ২: একই  *  দুই জায়গায় দুই অর্থ
--------------------------------------------------------------------------------

    int *p = &x;    // এখানে *  মানে  "p একটা pointer"  (declaration)
    *p = 50;        // এখানে *  মানে  "value-তে যাও"     (dereference)

  চেনার সহজ নিয়ম:

      লাইনের শুরুতে type আছে (int, double...)  ->  declaration
      type নেই, শুধু  *p                       ->  dereference

  আরেকটা ফাঁদ:

    int *p;
    p  = &x;        // [OK] ঠিক — p-এর ভিতরে address রাখছি
    *p = &x;        // [X] ভুল — *p হলো int, তার ভিতরে address রাখা যায় না


--------------------------------------------------------------------------------
  ২.৪  ফাঁদ ৩: type মিলতেই হবে
--------------------------------------------------------------------------------

    int    x = 10;
    double d = 3.5;

    int    *p = &x;      // [OK]
    double *q = &d;      // [OK]

    int    *r = &d;      // [X] error: double-এর address int-pointer-এ যাবে না

  নিয়ম:  যেই type-এর pointer, সেই type-এর variable-কেই point করবে।


--------------------------------------------------------------------------------
  ২.৫  জটিল declaration পড়ার নিয়ম  (Right-Left Rule)
--------------------------------------------------------------------------------

  নামের জায়গা থেকে শুরু করে ডানে-বামে দোল খেয়ে পড়ো:

    int *p;              ->  p হলো int-এর pointer
    int **pp;            ->  pp হলো int-pointer-এর pointer
    int *arr[5];         ->  arr হলো ৫টা int-pointer-এর array
    int (*p)[5];         ->  p হলো ৫ ঘরের int array-এর pointer
    int (*f)(int);       ->  f হলো এমন function-এর pointer, যেটা int নেয়
                             আর int ফেরত দেয়

  এখনই মুখস্থ করার দরকার নেই — PART 12 পর্যন্ত গেলে আপনাআপনি বুঝবে।


################################################################################
#  PART 3 — NULL, UNINITIALIZED আর DANGLING POINTER
#           (৯০% pointer crash এই তিনটার জন্যই হয়)
################################################################################

--------------------------------------------------------------------------------
  ৩.১  Uninitialized pointer — সবচেয়ে বিপদজনক
--------------------------------------------------------------------------------

    int *p;          // p-এর ভিতরে কী আছে? কেউ জানে না (garbage address)
    *p = 10;         // [X] CRASH / undefined behaviour

  কেন? কারণ p এমন একটা random ঠিকানায় point করছে, যেটা তোমার নয়।
       সেখানে লিখতে গেলে program মরে যায় — বা আরও খারাপ, চুপচাপ
       অন্য জায়গার data নষ্ট করে দেয়।

  নিয়ম:  pointer বানানোর সময়ই হয় address দাও, নাহয় nullptr দাও।

    int *p = nullptr;    // [OK] মানে "আমি এখন কোথাও point করছি না"


--------------------------------------------------------------------------------
  ৩.২  NULL pointer কী?
--------------------------------------------------------------------------------

  NULL pointer মানে — "এই pointer এখন কোনো valid জায়গায় point করছে না"

    int *p = nullptr;    // [OK] modern C++ (C++11 থেকে) — এটাই ব্যবহার করো
    int *p = NULL;       // পুরোনো C স্টাইল, কাজ করে
    int *p = 0;          // কাজ করে, কিন্তু বিভ্রান্তিকর

  ব্যবহার করার আগে সবসময় check করো:

    if (p != nullptr) {
        cout << *p;      // এখন নিরাপদ
    }

    if (p) cout << *p;   // ছোট করে এভাবেও লেখা যায়

  [X] কখনো nullptr dereference করো না:

    int *p = nullptr;
    cout << *p;          // CRASH (segmentation fault)


--------------------------------------------------------------------------------
  ৩.৩  Dangling pointer — যেই বাসা আর নেই, তার ঠিকানা
--------------------------------------------------------------------------------

  তিনভাবে হতে পারে —

  (ক) delete করার পরেও pointer রেখে দিলে:

        int *p = new int(10);
        delete p;            // memory ফেরত দিয়ে দিলাম
        cout << *p;          // [X] dangling — p এখনো ওই মৃত জায়গায় তাকিয়ে আছে

        // সমাধান:
        delete p;
        p = nullptr;         // [OK] delete করার পরপরই nullptr করে দাও

  (খ) local variable-এর address return করলে:

        int* bad() {
            int x = 10;
            return &x;       // [X] function শেষ হলেই x মরে যায়
        }

  (গ) scope শেষ হয়ে গেলে:

        int *p;
        {
            int x = 10;
            p = &x;
        }                    // x এখানেই শেষ
        cout << *p;          // [X] dangling


  +----------------------------------------------------------------------+
  |  মনে রাখো (PART 3)                                                   |
  |   ১. pointer সবসময় initialize করো (address বা nullptr দিয়ে)         |
  |   ২. dereference করার আগে nullptr check করো                          |
  |   ৩. delete করার পরপরই  p = nullptr;  লিখে দাও                       |
  +----------------------------------------------------------------------+


################################################################################
#  PART 4 — POINTER আর FUNCTION
#           (pointer আসলে কেন দরকার — উত্তরটা এই PART-এ)
################################################################################

--------------------------------------------------------------------------------
  ৪.১  সমস্যা: function-এর ভিতরে বদলালে বাইরে বদলায় না
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    void change(int a) {      // a হলো x-এর একটা COPY
        a = 100;
    }

    int main() {
        int x = 10;
        change(x);
        cout << x;            // 10  <- বদলায়নি!
        return 0;
    }

  কারণ (Pass by Value):
      function-কে x দেওয়া হয়নি, x-এর ফটোকপি দেওয়া হয়েছে।
      ফটোকপিতে লিখলে আসল কাগজ বদলায় না।


--------------------------------------------------------------------------------
  ৪.২  সমাধান ১: Pass by Pointer  (address পাঠাও)
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    void change(int *p) {     // p-এর ভিতরে x-এর address আছে
        *p = 100;             // address-এ গিয়ে আসল x বদলালাম
    }

    int main() {
        int x = 10;
        change(&x);           // address পাঠাচ্ছি, তাই  &  দিলাম
        cout << x;            // 100  <- বদলে গেছে!
        return 0;
    }

  ফটোকপি নয়, এবার বাসার ঠিকানা পাঠালাম — তাই আসল বাসাতেই কাজ হলো।


--------------------------------------------------------------------------------
  ৪.৩  সমাধান ২: Pass by Reference  (C++ এর সহজ উপায়)
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    void change(int &a) {     // a হলো x-এর আরেকটা নাম (alias)
        a = 100;              //  *  লাগলো না
    }

    int main() {
        int x = 10;
        change(x);            //  &  লাগলো না
        cout << x;            // 100
        return 0;
    }

  Reference = pointer-এর সহজ চেহারা। ভিতরে কাজ একই, শুধু লেখা কম।


--------------------------------------------------------------------------------
  ৪.৪  তিনটা একসাথে তুলনা
--------------------------------------------------------------------------------

  | পদ্ধতি        | Declaration  | যেভাবে call | ভিতরে লিখবে | আসল বদলায়? |
  |---------------|--------------|-------------|-------------|-------------|
  | by value      | f(int a)     | f(x)        | a           | [X] না      |
  | by pointer    | f(int *p)    | f(&x)       | *p          | [OK] হ্যাঁ  |
  | by reference  | f(int &a)    | f(x)        | a           | [OK] হ্যাঁ  |


--------------------------------------------------------------------------------
  ৪.৫  বিখ্যাত উদাহরণ: swap দুইভাবে
--------------------------------------------------------------------------------

    // pointer দিয়ে
    void swapPtr(int *a, int *b) {
        int temp = *a;
        *a = *b;
        *b = temp;
    }

    // reference দিয়ে
    void swapRef(int &a, int &b) {
        int temp = a;
        a = b;
        b = temp;
    }

    int main() {
        int x = 1, y = 2;

        swapPtr(&x, &y);      // x=2, y=1
        swapRef(x, y);        // x=1, y=2  (আবার উল্টে গেলো)
    }


--------------------------------------------------------------------------------
  ৪.৬  একটা function থেকে একাধিক উত্তর ফেরানো
--------------------------------------------------------------------------------

  return দিয়ে একটাই value ফেরানো যায়। কিন্তু pointer/reference পাঠালে
  যত ইচ্ছা উত্তর ফেরানো যায়:

    void minMax(int arr[], int n, int *mn, int *mx) {
        *mn = arr[0];
        *mx = arr[0];
        for (int i = 1; i < n; i++) {
            if (arr[i] < *mn) *mn = arr[i];
            if (arr[i] > *mx) *mx = arr[i];
        }
    }

    int main() {
        int a[5] = {4, 2, 9, 1, 7};
        int small, big;

        minMax(a, 5, &small, &big);

        cout << small << " " << big;   // 1 9
    }


  +----------------------------------------------------------------------+
  |  মনে রাখো (PART 4)                                                   |
  |  Function-এর ভিতর থেকে বাইরের জিনিস বদলাতে হলে                       |
  |  copy নয় — address (pointer) বা alias (reference) পাঠাও              |
  +----------------------------------------------------------------------+


################################################################################
#  PART 5 — POINTER আর ARRAY  (pointer arithmetic)
################################################################################

--------------------------------------------------------------------------------
  ৫.১  Array-এর নাম আসলে একটা address
--------------------------------------------------------------------------------

    int arr[5] = {10, 20, 30, 40, 50};

    cout << arr;        // 0x1000  <- প্রথম element-এর address
    cout << &arr[0];    // 0x1000  <- হুবহু একই জিনিস!

  মানে:   arr  ==  &arr[0]

  Array-এর element গুলো memory-তে পাশাপাশি বসে — এটাই array-র জাদু:

      index :    0      1      2      3      4
             +------+------+------+------+------+
      value  |  10  |  20  |  30  |  40  |  50  |
             +------+------+------+------+------+
      addr   | 1000 | 1004 | 1008 | 1012 | 1016 |
                        ( int = ৪ byte, তাই ৪ করে বাড়ছে )


--------------------------------------------------------------------------------
  ৫.২  Pointer Arithmetic —  p+1  মানে "পরের element"
--------------------------------------------------------------------------------

    int arr[5] = {10, 20, 30, 40, 50};
    int *p = arr;          //  &  লাগে না, arr নিজেই তো address

    cout << p     << endl;   // 1000
    cout << p + 1 << endl;   // 1004  <- 1001 নয়!  (৪ byte লাফ দিলো)
    cout << p + 2 << endl;   // 1008

    cout << *p       << endl;   // 10
    cout << *(p + 1) << endl;   // 20
    cout << *(p + 2) << endl;   // 30

  খুব গুরুত্বপূর্ণ:
      p + 1  মানে "১ byte সামনে" নয়,
      p + 1  মানে "১টা element সামনে"  =  sizeof(type) byte সামনে

      int    *p   ->  p+1 হলে  ৪ byte সামনে
      double *q   ->  q+1 হলে  ৮ byte সামনে
      char   *c   ->  c+1 হলে  ১ byte সামনে


--------------------------------------------------------------------------------
  ৫.৩   arr[i]  আসলে  *(arr + i)
--------------------------------------------------------------------------------

  Compiler ভিতরে ভিতরে এটাই করে:

      arr[i]     ==     *(arr + i)

  তাই এই চারটা লাইন হুবহু একই জিনিস:

    cout << arr[2];
    cout << *(arr + 2);
    cout << p[2];          // pointer-এর উপরেও  []  চলে!
    cout << *(p + 2);

  মজার ব্যাপার (বাস্তবে লিখবে না):  2[arr]  ও কাজ করে,
  কারণ  *(2 + arr)  আর  *(arr + 2)  একই কথা।


--------------------------------------------------------------------------------
  ৫.৪  Pointer দিয়ে array ঘোরানো — ৩ স্টাইল
--------------------------------------------------------------------------------

    int arr[5] = {10, 20, 30, 40, 50};

    // স্টাইল ১: index দিয়ে (পড়তে সবচেয়ে সহজ)
    for (int i = 0; i < 5; i++)
        cout << arr[i] << " ";

    // স্টাইল ২: pointer নিজেই সামনে এগোচ্ছে
    for (int *p = arr; p < arr + 5; p++)
        cout << *p << " ";

    // স্টাইল ৩: pointer এক জায়গায় স্থির, index যোগ হচ্ছে
    int *p = arr;
    for (int i = 0; i < 5; i++)
        cout << *(p + i) << " ";

  তিনটাই print করবে:   10 20 30 40 50


--------------------------------------------------------------------------------
  ৫.৫  দুইটা pointer বিয়োগ করলে দূরত্ব পাওয়া যায়
--------------------------------------------------------------------------------

    int arr[5] = {10, 20, 30, 40, 50};

    int *a = &arr[1];
    int *b = &arr[4];

    cout << b - a;      // 3   <- কয়টা element দূরে (byte নয়)

  যোগ করা যায় না (  a + b  ভুল ), কিন্তু বিয়োগ আর তুলনা করা যায়:

    if (a < b)  cout << "a আগে আছে";


--------------------------------------------------------------------------------
  ৫.৬  বড় ফাঁদ: array function-এ পাঠালে size হারিয়ে যায়  (decay)
--------------------------------------------------------------------------------

    void show(int arr[]) {         // ভিতরে arr আসলে int* হয়ে গেছে
        cout << sizeof(arr);       // 8  <- pointer-এর size!
    }

    int main() {
        int a[5];
        cout << sizeof(a);         // 20  <- 5 * 4, আসল array
        show(a);                   // 8
    }

  কারণ: array পাঠালে পুরো array copy হয় না, শুধু প্রথম element-এর
        address যায়। এটাকে বলে "array decays to pointer"।

  তাই size সবসময় আলাদা করে পাঠাও:

    void show(int arr[], int n) {      //  int *arr  লিখলেও একই কথা
        for (int i = 0; i < n; i++)
            cout << arr[i] << " ";
    }

  এখানে আরেকটা জিনিসও বোঝা গেলো: function-এর ভিতরে array বদলালে
  বাইরেও বদলে যায় (কারণ address-ই গেছে, copy যায়নি):

    void doubleAll(int arr[], int n) {
        for (int i = 0; i < n; i++)
            arr[i] *= 2;               // আসল array-ই বদলাচ্ছে
    }


  +----------------------------------------------------------------------+
  |  মনে রাখো (PART 5)                                                   |
  |      arr      ==  &arr[0]                                            |
  |      arr[i]   ==  *(arr + i)                                         |
  |      p + 1    ==  পরের element (sizeof(type) byte সামনে)             |
  |      array function-এ পাঠালে size হারায়  ->  size আলাদা পাঠাও        |
  +----------------------------------------------------------------------+


################################################################################
#  PART 6 — POINTER আর STRING  ( char* )
################################################################################

--------------------------------------------------------------------------------
  ৬.১  C-style string = char array, শেষে  '\0'
--------------------------------------------------------------------------------

    char s[6] = "Hello";

             +---+---+---+---+---+----+
             | H | e | l | l | o | \0 |
             +---+---+---+---+---+----+
   index       0   1   2   3   4   5

  '\0'  (null character) মানে — "string এখানেই শেষ"।
  তাই "Hello" ৫ অক্ষর হলেও ৬ byte জায়গা লাগে।


--------------------------------------------------------------------------------
  ৬.২  char pointer দিয়ে string
--------------------------------------------------------------------------------

    const char *s = "Hello";     // const লেখো — literal বদলানো যায় না

    cout << s;        // Hello   <- char* হলে cout পুরো string ছাপে!
    cout << *s;       // H       <- প্রথম অক্ষর
    cout << s[1];     // e
    cout << *(s + 1); // e

  খেয়াল করো:  int* ছাপলে address আসে, কিন্তু char* ছাপলে পুরো
              string আসে। এটা cout-এর বিশেষ ব্যবহার।

  address দেখতে চাইলে cast করতে হয়:

    cout << (void*)s;     // 0x...


--------------------------------------------------------------------------------
  ৬.৩  নিজের হাতে string-এর length বের করা
--------------------------------------------------------------------------------

    int myLength(const char *s) {
        int count = 0;
        while (*s != '\0') {      // যতক্ষণ শেষ চিহ্ন না আসে
            count++;
            s++;                  // পরের অক্ষরে যাও
        }
        return count;
    }

    int main() {
        cout << myLength("Hello");    // 5
    }

  আসল  strlen()  ভিতরে এই কাজটাই করে।


--------------------------------------------------------------------------------
  ৬.৪  Modern C++ এ আসলে কী করবে?
--------------------------------------------------------------------------------

  char* string পুরোনো C-এর জিনিস। C++ এ  std::string  ব্যবহার করো:

    #include <string>
    string s = "Hello";
    s += " World";              // সহজ, নিরাপদ, size নিজেই সামলায়

  char* শুধু তখন লাগে — পুরোনো C library, competitive programming-এ
  খুব বড় input, বা interview-তে "নিজে strlen লেখো" ধরনের প্রশ্নে।


################################################################################
#  PART 7 — POINTER আর struct / class  (  ->  operator )
################################################################################

--------------------------------------------------------------------------------
  ৭.১  struct pointer আর তীর চিহ্ন
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    struct Student {
        string name;
        int    age;
    };

    int main() {
        Student s = {"Mehedi", 22};
        Student *p = &s;

        // object দিয়ে হলে — ডট
        cout << s.name << endl;

        // pointer দিয়ে হলে — দুইভাবে লেখা যায়
        cout << (*p).name << endl;    // ভিতরে ঢুকে, তারপর ডট (bracket লাগবে)
        cout << p->name   << endl;    // [OK] সবাই এইটাই লেখে

        return 0;
    }

  নিয়ম:       p->name     ==     (*p).name

      .   ->  object-এর জন্য
      ->  ->  pointer-এর জন্য

  (*p).name-এ bracket বাধ্যতামূলক, কারণ  .  আগে চলে যায়  *  এর চেয়ে।
  এই ঝামেলা এড়াতেই  ->  বানানো হয়েছে।


--------------------------------------------------------------------------------
  ৭.২  Pointer দিয়ে struct বদলানো
--------------------------------------------------------------------------------

    void birthday(Student *p) {
        p->age++;                  // আসল object বদলাচ্ছে
    }

    int main() {
        Student s = {"Mehedi", 22};
        birthday(&s);
        cout << s.age;             // 23
    }

  বড় struct-এ pointer বা reference পাঠানো জরুরি — নাহয় পুরো struct
  copy হয়, যা অনেক ধীর।


--------------------------------------------------------------------------------
  ৭.৩  নিজের দিকেই তাকানো struct — Linked List-এর ভিত্তি
--------------------------------------------------------------------------------

  একটা struct নিজের type-এর pointer রাখতে পারে। এই একটা কৌশল দিয়েই
  linked list, tree, graph — সব বানানো হয়:

    struct Node {
        int  data;
        Node *next;        // পরের node কোথায় আছে
    };

    int main() {
        Node a, b, c;

        a.data = 1;  a.next = &b;
        b.data = 2;  b.next = &c;
        c.data = 3;  c.next = nullptr;    // শেষ node

        // পুরো list ঘুরে দেখা
        Node *cur = &a;
        while (cur != nullptr) {
            cout << cur->data << " -> ";
            cur = cur->next;
        }
        cout << "NULL";
        // Output: 1 -> 2 -> 3 -> NULL
    }

  ছবিতে:

      +---+---+     +---+---+     +---+------+
      | 1 | *-|---->| 2 | *-|---->| 3 | NULL |
      +---+---+     +---+---+     +---+------+
        a             b             c

  Array-তে element গুলো পাশাপাশি থাকতে বাধ্য। Linked list-এ থাকতে
  হয় না — pointer দিয়ে জোড়া লাগানো থাকে। এটাই pointer-এর সবচেয়ে
  বড় ব্যবহার।


################################################################################
#  PART 8 — POINTER TO POINTER  ( int **pp )
################################################################################

--------------------------------------------------------------------------------
  ৮.১  ধারণা
--------------------------------------------------------------------------------

  Pointer to pointer = এমন pointer যেটা আরেকটা pointer-এর address ধরে রাখে

      x   ->  value ধরে
      p   ->  x-এর address ধরে
      pp  ->  p-এর address ধরে

  সহজ উদাহরণ:

      x   =  টাকা
      p   =  টাকা কোথায় আছে, তার ঠিকানা
      pp  =  ওই ঠিকানার কাগজটা কোথায় আছে, তার ঠিকানা


--------------------------------------------------------------------------------
  ৮.২  সহজ উদাহরণ
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    int main() {
        int x = 10;

        int *p   = &x;    // int-এর pointer
        int **pp = &p;    // pointer-এর pointer

        cout << x  << endl;    // 10
        cout << p  << endl;    // x-এর address
        cout << pp << endl;    // p-এর address

        cout << *p   << endl;  // 10
        cout << *pp  << endl;  // x-এর address (মানে p নিজে)
        cout << **pp << endl;  // 10

        return 0;
    }


--------------------------------------------------------------------------------
  ৮.৩  Memory-তে কীভাবে থাকে (ছবি)
--------------------------------------------------------------------------------

  ধরি:

      x  = 10    ->  address 1000
      p  = 1000  ->  address 2000
      pp = 2000  ->  address 3000

               pp                p                 x
        +-----------+     +-----------+     +-----------+
        |   2000    |---->|   1000    |---->|    10     |
        +-----------+     +-----------+     +-----------+
         addr 3000         addr 2000         addr 1000

  | Variable | Value | মানে           |
  |----------|-------|----------------|
  | x        | 10    | আসল data       |
  | p        | 1000  | x-এর address   |
  | pp       | 2000  | p-এর address   |


--------------------------------------------------------------------------------
  ৮.৪  Dereference চেইন  (সবচেয়ে important)
--------------------------------------------------------------------------------

  | যা লিখলাম | মানে                       |
  |------------|----------------------------|
  | p          | x-এর address               |
  | *p         | x-এর value  (10)           |
  | pp         | p-এর address               |
  | *pp        | p  (মানে x-এর address)     |
  | **pp       | x-এর value  (10)           |

  সহজ নিয়ম: প্রতিটা  *  তোমাকে এক ধাপ ভিতরে ঢোকায়।
             pp-তে ২টা তারা আছে, তাই value পেতে ২ বার ঢুকতে হবে।


--------------------------------------------------------------------------------
  ৮.৫  pointer to pointer দিয়ে value বদলানো
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    int main() {
        int x = 10;

        int *p   = &x;
        int **pp = &p;

        **pp = 50;

        cout << x << endl;   // 50

        return 0;
    }

  ব্যাখ্যা:   **pp = 50   ->   pp থেকে p, p থেকে x, তারপর value বদল


--------------------------------------------------------------------------------
  ৮.৬  আসল কাজ: function-এ pointer নিজেকেই বদলানো
--------------------------------------------------------------------------------

  [X] pointer to pointer ছাড়া — কাজ হবে না

    void change(int *p) {
        p = nullptr;       // p হলো copy, তাই বাইরের pointer বদলায় না
    }

  [OK] pointer to pointer দিয়ে

    #include <iostream>
    using namespace std;

    void change(int **pp) {
        *pp = nullptr;     // *pp মানেই আসল p, তাই সত্যিই বদলায়
    }

    int main() {
        int x  = 10;
        int *p = &x;

        change(&p);        // pointer-এর address পাঠালাম

        if (p == nullptr)
            cout << "Pointer is NULL";

        return 0;
    }

  মূল লজিক PART 4-এর মতোই:

      int বদলাতে চাইলে      ->  int-এর address লাগে       ( int*  )
      pointer বদলাতে চাইলে  ->  pointer-এর address লাগে   ( int** )


--------------------------------------------------------------------------------
  ৮.৭  কোথায় সত্যিই লাগে?
--------------------------------------------------------------------------------

      ১. function-এ pointer নিজেকে modify করতে (উপরের উদাহরণ)
      ২. Dynamic 2D array  (PART 9.6)
      ৩. Linked list-এ head বদলানোর function-এ
      ৪. C-এর  char **argv  (program-এর command line argument)

    int main(int argc, char **argv) {
        // argv হলো string-এর array = char-pointer-এর array
        cout << argv[0];        // program-এর নাম
    }


################################################################################
#  PART 9 — DYNAMIC MEMORY:  new / delete
#           (Stack vs Heap — pointer-এর সবচেয়ে বড় ব্যবহার)
################################################################################

--------------------------------------------------------------------------------
  ৯.১  Stack আর Heap
--------------------------------------------------------------------------------

  | বিষয়        | STACK                      | HEAP                       |
  |-------------|----------------------------|----------------------------|
  | কে বানায়   | compiler নিজেই             | তুমি,  new  দিয়ে           |
  | কে মুছে     | নিজেই মুছে যায়            | তুমি,  delete  দিয়ে        |
  | আয়ু        | scope শেষ = মৃত            | delete না করা পর্যন্ত বাঁচে |
  | size        | ছোট (~১-৮ MB)             | বড় (RAM যতটুকু আছে)       |
  | গতি         | খুব দ্রুত                  | তুলনামূলক ধীর             |
  | কখন লাগে    | সাধারণ সব কাজে             | size runtime-এ জানা গেলে   |

    void f() {
        int a = 10;              // stack — function শেষে আপনাআপনি মুছে যাবে
        int *p = new int(20);    // heap  — delete না করলে থেকেই যাবে
        delete p;                // নিজের হাতে মুছতে হবে
    }


--------------------------------------------------------------------------------
  ৯.২  heap-এ একটা variable বানানো
--------------------------------------------------------------------------------

    int *p = new int;        // জায়গা নিলাম, ভিতরে garbage
    *p = 10;

    int *q = new int(10);    // জায়গা নিলাম + 10 বসালাম (একসাথে)

    cout << *q;              // 10

    delete q;                // জায়গা ফেরত দিলাম
    q = nullptr;             // dangling এড়ালাম

  খেয়াল করো: heap-এ বানানো জিনিসের কোনো "নাম" থাকে না।
             একমাত্র pointer-ই তার ঠিকানা জানে।
             Pointer হারিয়ে গেলে জিনিসটা আর কখনো খুঁজে পাওয়া
             যাবে না — এটাকেই বলে memory leak।


--------------------------------------------------------------------------------
  ৯.৩  Runtime-এ array বানানো  (এইটার জন্যই তো new দরকার)
--------------------------------------------------------------------------------

  সমস্যা: সাধারণ array-র size আগে থেকেই জানতে হয়।

    int n;
    cin >> n;
    int arr[n];        // C++ standard-এ বৈধ নয় (কিছু compiler মেনে নেয়)

  সমাধান:

    #include <iostream>
    using namespace std;

    int main() {
        int n;
        cin >> n;

        int *arr = new int[n];        // [OK] runtime-এ n টা int

        for (int i = 0; i < n; i++)
            cin >> arr[i];            // pointer-এও  []  চলে (PART 5.3)

        for (int i = 0; i < n; i++)
            cout << arr[i] << " ";

        delete[] arr;                 // array মুছতে delete[] , শুধু delete নয়
        arr = nullptr;

        return 0;
    }


--------------------------------------------------------------------------------
  ৯.৪   delete  বনাম  delete[]    (ভুল করলে বিপদ)
--------------------------------------------------------------------------------

    int *a = new int;        ->   delete a;        // [OK]
    int *b = new int[10];    ->   delete[] b;      // [OK]

    int *c = new int[10];    ->   delete c;        // [X] undefined behaviour

  নিয়ম:   new    এর জোড়া   delete
           new[]  এর জোড়া   delete[]
           যতবার new, ঠিক ততবার delete — একবারও কম বা বেশি নয়


--------------------------------------------------------------------------------
  ৯.৫  Memory Leak — নীরব ঘাতক
--------------------------------------------------------------------------------

  Memory leak মানে — heap থেকে জায়গা নিলাম, কিন্তু ফেরত দিলাম না

    void leak() {
        int *p = new int(10);
        // delete নেই -> function শেষ, p মুছে গেলো,
        // কিন্তু heap-এর ওই ৪ byte চিরকালের জন্য আটকে রইলো
    }

    int main() {
        for (int i = 0; i < 1000000; i++)
            leak();          // RAM ধীরে ধীরে শেষ হয়ে যাবে
    }

  আরেকটা রূপ — pointer-কে অন্যদিকে ঘুরিয়ে দেওয়া:

    int *p = new int(10);
    p = new int(20);         // [X] প্রথম 10-এর ঠিকানা হারিয়ে গেলো = leak
                             // ঠিক নিয়ম: আগে delete p; তারপর নতুন new

  সহজ অভ্যাস: প্রতিটা  new  লেখার সাথে সাথেই ঠিক করে ফেলো তার
              delete  কোথায় হবে। (আর PART 15-এর smart pointer শিখে
              ফেললে এই ঝামেলাটাই আর থাকে না।)


--------------------------------------------------------------------------------
  ৯.৬  Dynamic 2D Array  (এখানেই  int**  লাগে)
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    int main() {
        int row = 3, col = 4;

        // ধাপ ১: row সংখ্যক int-pointer-এর array
        int **mat = new int*[row];

        // ধাপ ২: প্রতিটা row-এর জন্য col সংখ্যক int
        for (int i = 0; i < row; i++)
            mat[i] = new int[col];

        // ব্যবহার — সাধারণ 2D array-র মতোই
        for (int i = 0; i < row; i++)
            for (int j = 0; j < col; j++)
                mat[i][j] = i * col + j;

        cout << mat[2][3];      // 11

        // ধাপ ৩: মোছা — উল্টো ক্রমে (আগে ভিতরের, পরে বাইরের)
        for (int i = 0; i < row; i++)
            delete[] mat[i];
        delete[] mat;

        return 0;
    }

  ছবিতে:

        mat          mat[0] --> [ 0 ][ 1 ][ 2 ][ 3 ]
      +------+
      | *----|-----> mat[1] --> [ 4 ][ 5 ][ 6 ][ 7 ]
      +------+
                     mat[2] --> [ 8 ][ 9 ][10 ][11 ]

  mat[i][j]  আসলে   *(*(mat + i) + j)

  বাস্তবে C++ এ এত কষ্ট না করে লিখবে:

    vector<vector<int>> mat(row, vector<int>(col));   // অনেক সহজ ও নিরাপদ

  তবু  int**  বোঝা দরকার — interview-তে আসে, আর পুরোনো C code
  পড়তে লাগে।


  +----------------------------------------------------------------------+
  |  মনে রাখো (PART 9)                                                   |
  |   new     ->  heap-এ জায়গা নেয়, address ফেরত দেয়                   |
  |   delete  ->  জায়গা ফেরত দেয় (value মুছে না, জায়গা ছাড়ে)          |
  |   new[] এর জোড়া delete[] ,  প্রতি new-এর ঠিক একটা delete            |
  +----------------------------------------------------------------------+


################################################################################
#  PART 10 — const POINTER বনাম POINTER TO const
#            (interview-এর সবচেয়ে প্রিয় প্রশ্ন)
################################################################################

--------------------------------------------------------------------------------
  ১০.১  আগে বুঝি const মানে কী
--------------------------------------------------------------------------------

  const মানে — এই জিনিস আর বদলানো যাবে না

    const int x = 10;
    x = 20;        // [X] error

  Pointer-এ বদলানোর মতো দুইটা জিনিস আছে:

      ১. pointer কোথায় point করছে       ->   p = &y;
      ২. যেখানে point করছে, সেখানের value ->   *p = 50;

  const কোনটাকে আটকাবে, সেটা ঠিক হয় const কোথায় বসেছে তার উপর।


--------------------------------------------------------------------------------
  ১০.২  Pointer to const  ( const int *p )  ->  VALUE আটকানো
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    int main() {
        int x = 10;
        int y = 20;

        const int *p = &x;

        // *p = 50;    [X] হবে না — value বদলানো যাবে না
        p = &y;        // [OK] হবে — কোথায় তাকাবে সেটা বদলানো যাবে

        cout << *p;    // 20
    }

  | যা লিখলাম | চলবে? | কারণ                       |
  |------------|-------|----------------------------|
  | *p = 50    | [X]   | value const                |
  | p = &y     | [OK]  | pointer বদলানো যায়        |

  বাস্তব ব্যবহার: function-এ বড় data পাঠাচ্ছি, copy চাই না, কিন্তু
                 function যেন data নষ্ট করতে না পারে —

    void print(const int *arr, int n) {
        for (int i = 0; i < n; i++)
            cout << arr[i];        // পড়তে পারবে
        // arr[0] = 5;             // [X] বদলাতে পারবে না — নিরাপদ
    }


--------------------------------------------------------------------------------
  ১০.৩  const pointer  ( int *const p )  ->  ADDRESS আটকানো
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    int main() {
        int x = 10;
        int y = 20;

        int *const p = &x;     // বানানোর সময়ই address দিয়ে দিতে হবে

        *p = 50;       // [OK] হবে — value বদলানো যাবে
        // p = &y;     [X] হবে না — pointer একদম fixed

        cout << x;     // 50
    }

  | যা লিখলাম | চলবে? | কারণ                       |
  |------------|-------|----------------------------|
  | *p = 50    | [OK]  | value বদলানো যায়         |
  | p = &y     | [X]   | pointer const              |


--------------------------------------------------------------------------------
  ১০.৪  দুইটাই const  ( const int *const p )
--------------------------------------------------------------------------------

    int x = 10, y = 20;
    const int *const p = &x;

    // *p = 20;    [X] value বদলানো যাবে না
    // p  = &y;    [X] address-ও বদলানো যাবে না

    cout << *p;     // শুধু পড়া যাবে


--------------------------------------------------------------------------------
  ১০.৫  সহজ ট্রিক  (পরীক্ষার হ্যাক)
--------------------------------------------------------------------------------

  নিয়ম:  const  যেই জিনিসের পাশে বসে, সেটাকেই আটকায়

    const int *p;      ->  const আছে int-এর পাশে      ->  value protected
    int *const p;      ->  const আছে pointer-এর পাশে  ->  pointer protected

  আরেকটা ট্রিক — ডান থেকে বামে পড়ো (  *  পড়ো "pointer to" হিসেবে):

    const int *p;    ->  "p is a pointer to an int that is const"
    int *const p;    ->  "p is a const pointer to an int"


--------------------------------------------------------------------------------
  ১০.৬  সারসংক্ষেপ টেবিল
--------------------------------------------------------------------------------

  | Type                | Value বদলানো যাবে? | Pointer বদলানো যাবে? |
  |---------------------|---------------------|-----------------------|
  | int *p              | [OK]                | [OK]                  |
  | const int *p        | [X]                 | [OK]                  |
  | int *const p        | [OK]                | [X]                   |
  | const int *const p  | [X]                 | [X]                   |

  সোনালী লাইন:

      Pointer to const  ->  value fixed
      const pointer     ->  address fixed

  Interview-এ প্রশ্ন করে:
      "difference between  const int *p  and  int *const p ?"

  উত্তর:
      প্রথমটায় value constant, দ্বিতীয়টায় pointer constant।
      (English-এ:  first -> value constant,  second -> pointer constant)


################################################################################
#  PART 11 — void POINTER
################################################################################

  void*  =  এমন pointer, যেটা যেকোনো type-এর address ধরতে পারে

    int    x = 10;
    double d = 3.5;

    void *p;

    p = &x;      // [OK]
    p = &d;      // [OK] — একই pointer, ভিন্ন type

  কিন্তু সমস্যা: type জানা নেই, তাই সরাসরি dereference করা যায় না

    cout << *p;              // [X] error

    cout << *(int*)p;        // [OK] cast করে বলে দিতে হবে "এটা int"

  পুরো উদাহরণ:

    #include <iostream>
    using namespace std;

    int main() {
        int x = 10;
        void *p = &x;

        cout << *(int*)p;    // 10

        return 0;
    }

  কোথায় লাগে?
      - C-এর  malloc()  void* ফেরত দেয়
      - C-এর  qsort()  যেকোনো type নিতে void* ব্যবহার করে
      - low-level বা generic code-এ

  C++ এ template থাকায় void*-এর দরকার অনেক কমে গেছে। জেনে রাখো,
  কিন্তু নিজের কোডে খুব একটা লাগবে না।


################################################################################
#  PART 12 — FUNCTION POINTER
################################################################################

--------------------------------------------------------------------------------
  ১২.১  Function-এরও address আছে
--------------------------------------------------------------------------------

  Function ও তো memory-তেই থাকে, তাই তারও একটা address আছে।
  সেই address ধরে রাখা pointer-কেই বলে function pointer।

    #include <iostream>
    using namespace std;

    int add(int a, int b) { return a + b; }
    int mul(int a, int b) { return a * b; }

    int main() {
        // লেখার নিয়ম:  return_type (*নাম)(parameter type গুলো)
        int (*fp)(int, int);

        fp = add;            // বা  fp = &add;  — দুইটাই চলে
        cout << fp(2, 3);    // 5      (  (*fp)(2,3)  লিখলেও একই )

        fp = mul;            // একই pointer, এখন অন্য function ধরলো
        cout << fp(2, 3);    // 6

        return 0;
    }

  bracket এখানে খুব জরুরি:

    int (*fp)(int, int);     // fp হলো function-এর pointer      [OK]
    int *fp(int, int);       // fp হলো function যেটা int* ফেরত দেয় (আলাদা!)


--------------------------------------------------------------------------------
  ১২.২  Function-কেই argument হিসেবে পাঠানো
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    int add(int a, int b) { return a + b; }
    int mul(int a, int b) { return a * b; }

    // "কোন কাজটা করবে" সেটাও এখন একটা parameter
    int calc(int a, int b, int (*op)(int, int)) {
        return op(a, b);
    }

    int main() {
        cout << calc(3, 4, add) << endl;    // 7
        cout << calc(3, 4, mul) << endl;    // 12
        return 0;
    }

  এটাই callback-এর ধারণা — "কী কাজ করবে" সেটাকেও data বানিয়ে পাঠানো।


--------------------------------------------------------------------------------
  ১২.৩  বাস্তব ব্যবহার: নিজের নিয়মে sort
--------------------------------------------------------------------------------

    #include <iostream>
    #include <algorithm>
    using namespace std;

    bool desc(int a, int b) { return a > b; }

    int main() {
        int arr[5] = {3, 1, 4, 1, 5};

        sort(arr, arr + 5, desc);       // desc এখানে function pointer

        for (int i = 0; i < 5; i++)
            cout << arr[i] << " ";      // 5 4 3 1 1
    }

  আধুনিক C++ এ এই জায়গায় lambda বেশি ব্যবহৃত হয়:

    sort(arr, arr + 5, [](int a, int b) { return a > b; });

  আরও modern উপায় আছে —  #include <functional>  আর
  function<int(int,int)>  — কিন্তু ভিতরের ধারণা এই function pointer-ই।


################################################################################
#  PART 13 — this POINTER  (OOP)
################################################################################

--------------------------------------------------------------------------------
  ১৩.১  this কী?
--------------------------------------------------------------------------------

  this হলো এমন একটা pointer, যেটা CURRENT OBJECT-কে point করে।
  প্রতিটা member function-এর ভিতরে এটা আপনাআপনিই থাকে।

  সহজ উদাহরণ:
      ক্লাসে teacher বললো — "this student stand up"
      এখানে "this" মানে যেই student-এর কথা এখন হচ্ছে।
      ঠিক তেমনি  this = যেই object-এর উপর function এখন চলছে।


--------------------------------------------------------------------------------
  ১৩.২  সমস্যা: parameter আর member-এর নাম একই
--------------------------------------------------------------------------------

    class Student {
    public:
        int age;

        void setAge(int age) {
            age = age;        // [X] parameter নিজেকেই নিজে বসালো,
        }                     //     member age অচ্ছুৎ পড়ে রইলো
    };

  কারণ: ভিতরের নাম (parameter) বাইরের নাম (member) ঢেকে দেয়।


--------------------------------------------------------------------------------
  ১৩.৩  সমাধান:  this->
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    class Student {
    public:
        int age;

        void setAge(int age) {
            this->age = age;      // this->age = member,  age = parameter
        }

        void print() {
            cout << age << endl;  // this->age লেখাও যেতো, দরকার নেই
        }
    };

    int main() {
        Student s;

        s.setAge(25);
        s.print();        // 25

        return 0;
    }


--------------------------------------------------------------------------------
  ১৩.৪  পর্দার আড়ালে কী হয়?  (এইটা বুঝলে this পুরো clear)
--------------------------------------------------------------------------------

  তুমি লিখো:

    s.setAge(25);

  Compiler ভিতরে ভিতরে করে:

    setAge(&s, 25);        // object-এর address গোপনে পাঠিয়ে দেয়

  মানে:   this = &s

  তাই this সবসময় একটা pointer, আর সেজন্যই  this->  লিখতে হয়,
  this.  নয়।

  Type:   class Student  হলে,  this  এর type হবে  Student*


--------------------------------------------------------------------------------
  ১৩.৫  Method Chaining  ( this return করা )
--------------------------------------------------------------------------------

    #include <iostream>
    using namespace std;

    class Student {
    public:
        string name;
        int    age;

        Student* setName(string n) { this->name = n; return this; }
        Student* setAge(int a)     { this->age  = a; return this; }
    };

    int main() {
        Student s;

        s.setName("Mehedi")->setAge(22);      // একসাথে চেইন!

        cout << s.name << " " << s.age;       // Mehedi 22
        return 0;
    }

  কাজ করছে কারণ setName আর setAge নিজের object-এর pointer ফেরত
  দিচ্ছে, তাই তার উপর আবার  ->  দিয়ে পরের কাজ করা যাচ্ছে।

  (Reference return করলে type হয়  Student& , তখন  .  দিয়ে চেইন হয় —
   বাস্তবে সেটাই বেশি প্রচলিত:   s.setName("x").setAge(22);  )


--------------------------------------------------------------------------------
  ১৩.৬  দুইটা object তুলনা করা
--------------------------------------------------------------------------------

    class Student {
    public:
        int age;

        void compare(Student s) {
            if (this->age > s.age)
                cout << "Current object বড়";
            else
                cout << "Parameter object বড়";
        }
    };


--------------------------------------------------------------------------------
  ১৩.৭  this-এর নিয়মগুলো
--------------------------------------------------------------------------------

      ১. this শুধু class-এর member function-এর ভিতরেই থাকে
         (বাইরে, সাধারণ function-এ নেই)

      ২. static function-এ this নেই

           static void f() {
               // this  [X] পাওয়া যাবে না — static function কোনো
               //                            নির্দিষ্ট object-এর নয়
           }

      ৩. this একটা pointer, তাই  this->age  (ডট নয়)


  +----------------------------------------------------------------------+
  |  মনে রাখো (PART 13)                                                  |
  |  this = current object-এর pointer,   type = ClassName*                |
  |  ব্যবহার: একই নামের variable, method chaining, object তুলনা          |
  |  এক লাইনে: "this is a pointer that points to the current object"     |
  +----------------------------------------------------------------------+


################################################################################
#  PART 14 — POINTER বনাম REFERENCE
################################################################################

--------------------------------------------------------------------------------
  ১৪.১  Reference কী?
--------------------------------------------------------------------------------

  Reference = একই variable-এর আরেকটা নাম (alias)

    int x = 10;
    int &r = x;      // r হলো x-এর ডাকনাম

    r = 50;
    cout << x;       // 50   <- একই জিনিস তো!

  নতুন কোনো variable তৈরি হয় না, শুধু নতুন একটা নাম হয়।


--------------------------------------------------------------------------------
  ১৪.২  পার্থক্যের টেবিল
--------------------------------------------------------------------------------

  | বিষয়                      | Pointer          | Reference        |
  |----------------------------|------------------|------------------|
  | Declaration                | int *p = &x;     | int &r = x;      |
  | ব্যবহারের সময় লিখি        | *p               | r                |
  | খালি রাখা যায়?            | হ্যাঁ (nullptr)  | না, লাগবেই      |
  | পরে অন্যদিকে ঘুরানো যায়?  | হ্যাঁ            | না, চিরকাল একই  |
  | NULL হতে পারে?             | হ্যাঁ            | না               |
  | Arithmetic ( p++ )         | হ্যাঁ            | না               |
  | নিজের আলাদা address আছে?  | হ্যাঁ            | নেই              |
  | পড়তে সহজ                  | কম               | বেশি             |

    int x = 10, y = 20;

    int *p = &x;   p = &y;      // [OK] pointer ঘুরানো গেলো
    int &r = x;    r = y;       // এটা ঘুরানো নয়! x-এর value 20 হয়ে গেলো


--------------------------------------------------------------------------------
  ১৪.৩  কোনটা কখন ব্যবহার করবে?  (কাজের নিয়ম)
--------------------------------------------------------------------------------

  Reference ব্যবহার করো যখন:
      - function-এ বড় object copy না করে পাঠাতে চাও
              void f(const vector<int> &v)      <- CP-তে খুব দরকারি
      - function-এর ভিতরে argument বদলাতে চাও
              void swap(int &a, int &b)
      - নিশ্চিত জানো "সবসময় একটা valid জিনিস থাকবে"

  Pointer ব্যবহার করো যখন:
      - "কিছুই নেই" অবস্থা দরকার  ( nullptr )
      - পরে অন্য জিনিসের দিকে ঘুরাতে হবে
      - dynamic memory লাগবে ( new / delete )
      - linked list / tree / graph বানাতে হবে
      - array-র উপর হাঁটতে হবে ( p++ )

  সহজ নিয়ম: C++ এ যেটা reference দিয়ে করা যায়, reference দিয়েই করো।
             না পারলে তখন pointer ধরো।


################################################################################
#  PART 15 — SMART POINTER  (Modern C++, C++11 থেকে)
#            new/delete-এর ঝামেলা থেকে মুক্তি
################################################################################

--------------------------------------------------------------------------------
  ১৫.১  সমস্যাটা কী ছিলো?
--------------------------------------------------------------------------------

    void f() {
        int *p = new int(10);
        if (somethingWrong) return;      // [X] delete হলো না -> leak
        delete p;
    }

  মানুষ delete ভুলে যায়, তাই C++ বললো — "মোছার কাজটা আমিই করে দিই"।

    #include <memory>       // smart pointer-এর জন্য এটা লাগবে


--------------------------------------------------------------------------------
  ১৫.২  unique_ptr — একজনেরই মালিকানা  (default পছন্দ)
--------------------------------------------------------------------------------

    #include <iostream>
    #include <memory>
    using namespace std;

    int main() {
        unique_ptr<int> p = make_unique<int>(10);

        cout << *p << endl;      // 10  <- সাধারণ pointer-এর মতোই ব্যবহার

        *p = 20;
        cout << *p << endl;      // 20

        return 0;                // delete লিখতেই হলো না!
    }                            // scope শেষ -> নিজেই মুছে গেলো

  বৈশিষ্ট্য:
      - copy করা যায় না (দুইজন মালিক হতে পারে না)
      - move করা যায়:   unique_ptr<int> q = move(p);   // এখন p খালি
      - overhead প্রায় শূন্য — raw pointer-এর মতোই দ্রুত

  struct-এর সাথে:

    struct Node { int data; };

    unique_ptr<Node> n = make_unique<Node>();
    n->data = 5;
    cout << n->data;         // 5    (  ->  আগের মতোই কাজ করে )


--------------------------------------------------------------------------------
  ১৫.৩  shared_ptr — অনেকে মিলে মালিকানা
--------------------------------------------------------------------------------

    #include <iostream>
    #include <memory>
    using namespace std;

    int main() {
        shared_ptr<int> a = make_shared<int>(10);

        cout << a.use_count() << endl;   // 1  <- কতজন ধরে আছে

        {
            shared_ptr<int> b = a;       // copy করা যায়
            cout << a.use_count();       // 2
        }                                // b-এর scope শেষ

        cout << a.use_count();           // 1

        return 0;
    }                                    // count 0 হলে memory মুক্ত

  কাজের ধরন: reference counting। শেষ মালিক চলে গেলে memory মুছে যায়।
  খরচ: count রাখার জন্য একটু বেশি memory আর সময় লাগে।

  আরেকটা আছে —  weak_ptr  — shared_ptr-এর circular reference সমস্যা
  ঠেকাতে ব্যবহার হয়। এখনই দরকার নেই, শুধু নামটা জেনে রাখো।


--------------------------------------------------------------------------------
  ১৫.৪  কোনটা বেছে নেবে?
--------------------------------------------------------------------------------

  | পরিস্থিতি                            | যা ব্যবহার করবে     |
  |--------------------------------------|---------------------|
  | সাধারণ variable, নিজের scope-এ       | কিছুই না, stack     |
  | heap লাগবে, মালিক একজন               | unique_ptr          |
  | heap লাগবে, মালিক একাধিক             | shared_ptr          |
  | শুধু দেখবে, মালিক নও                | raw pointer / ref   |
  | নতুন কোডে new/delete                 | এড়িয়ে চলো         |

  Modern C++ এর মূল কথা:  হাতে  new / delete  লেখা এখন শেষ পছন্দ।

  তবু raw pointer শেখা বাধ্যতামূলক — কারণ smart pointer ভিতরে
  raw pointer-ই ব্যবহার করে, আর interview/CP-তে raw pointer-ই আসে।


################################################################################
#  PART 16 — ITERATOR = বড় ভাই POINTER
################################################################################

  STL-এর iterator আসলে pointer-এরই আপন ভাই। একই operator, একই ধারণা:

    #include <iostream>
    #include <vector>
    using namespace std;

    int main() {
        vector<int> v = {10, 20, 30};

        vector<int>::iterator it = v.begin();

        cout << *it << endl;       // 10   <- dereference, pointer-এর মতোই
        it++;                      //      <- সামনে এগোনো
        cout << *it << endl;       // 20

        // পুরো ঘুরে দেখা
        for (auto it = v.begin(); it != v.end(); it++)
            cout << *it << " ";    // 10 20 30

        return 0;
    }

  মিলগুলো দেখো:

      *it       ->  value            (  *p    এর মতো )
      it++      ->  পরের element     (  p++   এর মতো )
      it->x     ->  member access    (  p->x  এর মতো )
      v.end()   ->  শেষের পরের জায়গা (nullptr-এর মতো "শেষ" সংকেত)

  তাই pointer বুঝলে iterator আলাদা করে শেখাই লাগে না।

  একটা জিনিস মনে রাখো: vector-এ push_back করলে ভিতরের memory সরে
  যেতে পারে, তখন আগের iterator বা pointer অচল হয়ে যায় (dangling):

    vector<int> v = {1, 2, 3};
    int *p = &v[0];
    v.push_back(4);        // memory সরে যেতে পারে
    cout << *p;            // [X] বিপদ — p এখন dangling হতে পারে


################################################################################
#  PART 17 — COMPETITIVE PROGRAMMING-এ POINTER আসলে কোথায় লাগে
################################################################################

  সত্যি কথা: CP-তে raw pointer খুব কম লাগে, vector/STL-ই যথেষ্ট।
  কিন্তু এই ৫ জায়গায় লাগে, বা খুব কাজে দেয় —

  ১) function-এ reference পাঠিয়ে copy বাঁচানো  ->  সবচেয়ে দরকারি

        void solve(vector<int> &v)         // [OK] copy হয় না, দ্রুত
        void solve(const vector<int> &v)   // [OK] copy হয় না + নিরাপদ
        void solve(vector<int> v)          // [X] পুরো vector copy — TLE-র কারণ

  ২) Linked List-এর সমস্যা (LeetCode/interview-তে খুব common)

        struct ListNode {
            int val;
            ListNode *next;
            ListNode(int x) : val(x), next(nullptr) {}
        };

        // list উল্টে দেওয়া — pointer-এর ক্লাসিক সমস্যা
        ListNode* reverse(ListNode *head) {
            ListNode *prev = nullptr;
            while (head) {
                ListNode *nxt = head->next;
                head->next = prev;
                prev = head;
                head = nxt;
            }
            return prev;
        }

  ৩) Tree / Trie / Segment tree-র node

        struct TreeNode {
            int val;
            TreeNode *left, *right;
            TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
        };

        int height(TreeNode *root) {
            if (root == nullptr) return 0;                 // base case
            return 1 + max(height(root->left),
                           height(root->right));
        }

  ৪) Two-pointer technique — নাম pointer, কিন্তু এখানে index ব্যবহার হয়

        // sorted array-তে দুইটা সংখ্যা যোগ করে target হয় কি?
        int l = 0, r = n - 1;
        while (l < r) {
            int sum = a[l] + a[r];
            if (sum == target) break;
            if (sum < target) l++;
            else r--;
        }

     (এখানে l, r আসল pointer নয় — index। নাম মিললেও জিনিসটা আলাদা।)

  ৫) STL-এর iterator নেয় যেসব function

        sort(v.begin(), v.end());
        auto it = lower_bound(v.begin(), v.end(), x);
        int idx = it - v.begin();       // pointer বিয়োগের মতোই


################################################################################
#  PART 18 — সাধারণ ভুলের লিস্ট  (COMMON MISTAKES)
################################################################################

  ১) Uninitialized pointer dereference করা

        int *p;  *p = 10;                  // [X]
        int *p = nullptr;                  // [OK] আগে initialize করো

  ২) nullptr dereference করা

        if (p) cout << *p;                 // [OK] আগে check করো

  ৩) delete-এর পরেও ব্যবহার করা (dangling)

        delete p;  p = nullptr;            // [OK] সাথে সাথেই nullptr

  ৪) একই pointer দুইবার delete করা

        delete p;  delete p;               // [X] crash
        // nullptr করে রাখলে দ্বিতীয় delete নিরাপদ ( delete nullptr; বৈধ )

  ৫) delete[] এর বদলে delete দেওয়া

        int *a = new int[10];  delete[] a; // [OK]

  ৬) local variable-এর address return করা

        int* f() { int x = 5; return &x; } // [X]
        int* f() { return new int(5); }    // [OK] (caller delete করবে)

  ৭) new করে delete ভুলে যাওয়া (leak)

        // প্রতিটা new-এর জোড়া delete আছে কি? — মিলিয়ে দেখো

  ৮) এক লাইনে দুইটা pointer লেখা

        int* p, q;                         // [X] q pointer নয়
        int *p, *q;                        // [OK]

  ৯) Array-র সীমা পার হওয়া

        int a[5];  a[5] = 10;              // [X] index 0 থেকে 4 পর্যন্তই

 ১০) function-এর ভিতরে sizeof দিয়ে array-র size বের করা

        void f(int a[]) { sizeof(a); }     // [X] 8 আসবে, আসল size নয়

 ১১)  *p = &x   লিখে ফেলা

        int *p;  p = &x;                   // [OK]  (  *p = &x  ভুল )

 ১২) vector বদলানোর পর পুরোনো pointer ব্যবহার করা

        p = &v[0];  v.push_back(1);        // p এখন আর বিশ্বাসযোগ্য নয়


################################################################################
#  PART 19 — শেষ চিটশিট  (এক পাতায় সব)
################################################################################

--------------------------------------------------------------------------------
  লেখার নিয়ম (Syntax)
--------------------------------------------------------------------------------

    int x = 10;               // সাধারণ variable
    int *p = &x;              // pointer, x-এর address ধরলো
    int **pp = &p;            // pointer-এর pointer
    int &r = x;               // reference (x-এর ডাকনাম)

    p  = &x;                  // pointer-এ address বসানো
    *p = 50;                  // যেখানে point করছে, সেখানের value বদলানো

--------------------------------------------------------------------------------
  কোনটার মানে কী
--------------------------------------------------------------------------------

  | যা লিখি   | মানে                                       |
  |-----------|--------------------------------------------|
  | x         | value                                      |
  | &x        | x-এর address                               |
  | p         | address (যেখানে point করছে)                |
  | *p        | ওই address-এর value                        |
  | &p        | p নিজে কোথায় আছে                          |
  | **pp      | দুই ধাপ ভিতরের value                       |
  | p->m      | (*p).m  — pointer দিয়ে member access       |
  | p[i]      | *(p + i)                                   |
  | p + 1     | পরের element (sizeof(type) byte সামনে)     |

--------------------------------------------------------------------------------
  const
--------------------------------------------------------------------------------

    const int *p;          // value fixed,   address বদলানো যায়
    int *const p;          // address fixed, value বদলানো যায়
    const int *const p;    // দুইটাই fixed

--------------------------------------------------------------------------------
  Memory
--------------------------------------------------------------------------------

    int *p = new int(5);        delete p;      p = nullptr;
    int *a = new int[n];        delete[] a;    a = nullptr;
    unique_ptr<int> u = make_unique<int>(5);   // delete লাগে না

--------------------------------------------------------------------------------
  Function-এ কীভাবে পাঠাবে
--------------------------------------------------------------------------------

    void f(int a)                 // copy — বাইরে বদলাবে না
    void f(int *p)                // address — বদলাবে,  call: f(&x)
    void f(int &a)                // alias   — বদলাবে,  call: f(x)
    void f(const vector<int> &v)  // copy নেই + বদলাতেও পারবে না (সবচেয়ে ভালো)

--------------------------------------------------------------------------------
  সোনালী তিন লাইন
--------------------------------------------------------------------------------

      ১. pointer address রাখে,  *  দিয়ে value-তে পৌঁছাও
      ২. বাইরের জিনিস বদলাতে হলে address পাঠাও, copy নয়
      ৩. যা new করেছো তা delete করো — নাহয় smart pointer ব্যবহার করো


################################################################################
#  PART 20 — অনুশীলন প্রশ্ন + উত্তর
#            (আগে নিজে উত্তর ভাবো, তারপর নিচে দেখো)
################################################################################

--------------------------------------------------------------------------------
  প্রশ্ন ১
--------------------------------------------------------------------------------
    int x = 5;
    int *p = &x;
    *p = *p + 10;
    cout << x;
    // Output কী?

--------------------------------------------------------------------------------
  প্রশ্ন ২
--------------------------------------------------------------------------------
    int arr[4] = {1, 2, 3, 4};
    int *p = arr;
    p++;
    cout << *p << " " << *(p + 1);
    // Output কী?

--------------------------------------------------------------------------------
  প্রশ্ন ৩
--------------------------------------------------------------------------------
    int x = 10;
    int y = 20;
    const int *p = &x;
    int *const q = &x;
    // কোনগুলো বৈধ?
    //   (ক) *p = 5      (খ) p = &y
    //   (গ) *q = 5      (ঘ) q = &y

--------------------------------------------------------------------------------
  প্রশ্ন ৪
--------------------------------------------------------------------------------
    void f(int *p) { p = nullptr; }
    int main() {
        int x = 5;
        int *p = &x;
        f(p);
        cout << (p == nullptr);
    }
    // Output কী? আর কেন?

--------------------------------------------------------------------------------
  প্রশ্ন ৫
--------------------------------------------------------------------------------
    int x = 1;
    int *p = &x;
    int **pp = &p;
    **pp = 99;
    cout << x << " " << *p << " " << **pp;
    // Output কী?

--------------------------------------------------------------------------------
  প্রশ্ন ৬
--------------------------------------------------------------------------------
    // এই কোডে কী কী ভুল আছে? ৩টা খুঁজে বের করো
    int main() {
        int *p;
        *p = 10;
        int *a = new int[5];
        delete a;
    }

--------------------------------------------------------------------------------
  প্রশ্ন ৭
--------------------------------------------------------------------------------
    // sizeof কী কী print করবে?
    void f(int arr[]) { cout << sizeof(arr) << endl; }
    int main() {
        int a[10];
        cout << sizeof(a) << endl;
        f(a);
    }

--------------------------------------------------------------------------------
  প্রশ্ন ৮  (নিজে লেখার প্রশ্ন)
--------------------------------------------------------------------------------
    একটা function লেখো, যেটা pointer ব্যবহার করে দুইটা int-এর
    যোগফল আর গুণফল — দুইটাই একসাথে ফেরত দেয়।


================================================================================
  উত্তরপত্র  (ANSWERS)
================================================================================

  উত্তর ১)  15
      *p আর x একই memory, তাই *p বদলানো মানেই x বদলানো।

  উত্তর ২)  2 3
      p++ করার পর p এখন arr[1]-এ আছে, তাই *p = 2
      আর *(p+1) = arr[2] = 3।

  উত্তর ৩)  (ক) [X]  — const int *p, তাই value বদলানো যাবে না
            (খ) [OK] — pointer ঘুরানো যাবে
            (গ) [OK] — int *const q, তাই value বদলানো যাবে
            (ঘ) [X]  — pointer fixed, ঘুরানো যাবে না

  উত্তর ৪)  0  (মানে false)
      f-এর ভিতরের p হলো বাইরের p-এর COPY। copy-কে nullptr করলে
      আসল p বদলায় না। সত্যিই বদলাতে চাইলে  void f(int **pp)  লিখে
      f(&p)  করতে হবে (PART 8.6 দেখো)।

  উত্তর ৫)  99 99 99
      তিনটাই একই memory-র দিকে তাকিয়ে আছে।

  উত্তর ৬)  ১. int *p;  uninitialized — nullptr বা address দিতে হবে
            ২. *p = 10;  garbage address-এ লিখছে — crash / UB
            ৩. new int[5] এর জন্য  delete[] a;  লাগবে, শুধু delete নয়
            (৪ নম্বর ভুলও বলা যায়: delete-এর পর  a = nullptr;  নেই)

  উত্তর ৭)  40   <- main-এ, আসল array (10 * 4 byte)
            8    <- function-এ, array decay হয়ে pointer হয়ে গেছে

  উত্তর ৮)  void calc(int a, int b, int *sum, int *product) {
                *sum     = a + b;
                *product = a * b;
            }

            int main() {
                int s, p;
                calc(3, 4, &s, &p);
                cout << s << " " << p;      // 7 12
            }


================================================================================
  শেষ কথা
================================================================================

  Pointer কঠিন লাগে, কারণ একসাথে দুইটা জিনিস মাথায় রাখতে হয় —
  "কোথায়" (address) আর "কী" (value)।

  কোথাও আটকে গেলে সবসময় কাগজে বক্স এঁকে address আর value লিখে ফেলো।
  ছবি আঁকলে pointer-এর কোনো প্রশ্নই আর কঠিন থাকে না।

  আবার পড়তে হলে এই ক্রমে পড়ো:

      বেসিক          ->  PART 0, 1, 2, 3
      কাজে লাগানো    ->  PART 4, 5, 7
      Advanced        ->  PART 8, 9, 10
      Modern C++      ->  PART 14, 15, 16
      রিভিশন         ->  PART 19 (চিটশিট), PART 20 (অনুশীলন)

================================================================================
*/
